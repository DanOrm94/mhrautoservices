import { useEffect, useState } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import { Link, Route, Routes, useLocation, useNavigate } from 'react-router-dom'
import { ArrowRight, ArrowUpRight, CalendarDays, CarFront, CheckCircle2, ChevronDown, Clock3, Gauge, Instagram, Mail, MapPin, Menu, Phone, ShieldCheck, Sparkles, Wrench, X } from 'lucide-react'
import './admin.css'

const API = '/api'
const GOOGLE_REVIEWS_URL = 'https://share.google/Vx7BJ32uBduien6ND'
const services = [
  { icon: Wrench, title: 'Servicing', text: 'Manufacturer-minded servicing that protects performance, reliability and resale value.' },
  { icon: ShieldCheck, title: 'MOT', text: 'Straightforward MOT support with clear advice and no unnecessary upsells.' },
  { icon: Gauge, title: 'Diagnostics', text: 'Modern fault finding with live data and a proper root-cause approach.' },
  { icon: CarFront, title: 'Repairs', text: 'Brakes, suspension, exhausts, clutches and everyday repairs done properly.' },
]
const fallbackJobs = [
  { id: 1, title: 'Brake refresh', vehicle: 'BMW 3 Series', category: 'Brakes', description: 'Full brake replacement and inspection.', image_url: 'https://images.unsplash.com/photo-1489824904134-891ab64532f1?auto=format&fit=crop&w=1200&q=85' },
  { id: 2, title: 'Cooling system repair', vehicle: 'Ford Transit Custom', category: 'Repairs', description: 'Cooling system fault diagnosis and repair.', image_url: 'https://images.unsplash.com/photo-1503736334956-4c8f8e92946d?auto=format&fit=crop&w=1200&q=85' },
  { id: 3, title: 'Full service + diagnostics', vehicle: 'Audi A4', category: 'Servicing', description: 'Major service with digital diagnostic check.', image_url: 'https://images.unsplash.com/photo-1492144534655-ae79c964c9d7?auto=format&fit=crop&w=1200&q=85' },
  { id: 4, title: 'Suspension overhaul', vehicle: 'VW Golf GTI', category: 'Chassis', description: 'Suspension refresh and road test.', image_url: 'https://images.unsplash.com/photo-1511919884226-fd3cad34687c?auto=format&fit=crop&w=1200&q=85' },
]
type Job = typeof fallbackJobs[number]

function Shell({ children }: { children: React.ReactNode }) {
  const [open, setOpen] = useState(false)
  const location = useLocation()
  const nav = [['/', 'Home'], ['/work', 'Recent Work'], ['/mechanic', 'Meet the Mechanic'], ['/contact', 'Contact']]
  return <div className="app-shell"><header className="site-header"><Link to="/" className="brand" onClick={() => setOpen(false)} aria-label="MHR Auto Services home"><img className="brand-logo" src="/logo.jpg" alt="MHR Auto Services" /></Link><button className="menu-button" aria-label="Toggle navigation" onClick={() => setOpen(v => !v)}>{open ? <X size={22} /> : <Menu size={22} />}</button><nav className={`site-nav ${open ? 'open' : ''}`}>{nav.map(([href, label]) => <Link key={href} className={location.pathname === href ? 'active' : ''} to={href} onClick={() => setOpen(false)}>{label}</Link>)}<a href={`${GOOGLE_REVIEWS_URL}`} target="_blank" rel="noreferrer" className="nav-reviews">Reviews</a><Link to="/admin" className={`nav-admin ${location.pathname === '/admin' ? 'active' : ''}`} onClick={() => setOpen(false)}><ShieldCheck size={15} /> Admin</Link><Link to="/contact" className="nav-cta" onClick={() => setOpen(false)}>Book a Service <ArrowRight size={16} /></Link></nav></header><main>{children}</main><footer className="site-footer"><div><span className="eyebrow">MHR AUTO SERVICES</span><h3>Cars deserve more than guesswork.</h3><p>Independent mechanical care, modern diagnostics and honest advice.</p></div><div className="footer-links"><Link to="/work">Recent work</Link><Link to="/mechanic">Meet the mechanic</Link><Link to="/contact">Contact</Link><a href={GOOGLE_REVIEWS_URL} target="_blank" rel="noreferrer">Google reviews</a><Link to="/admin">Admin</Link></div><div className="footer-meta"><span>© 2026 MHR Auto Services</span><span>Built for the road ahead.</span></div></footer></div>
}
function Reveal({ children, delay = 0 }: { children: React.ReactNode, delay?: number }) { return <motion.div initial={{ opacity: 0, y: 24 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.18 }} transition={{ duration: 0.65, ease: 'easeOut', delay }}>{children}</motion.div> }

function Home() {
  const [jobs, setJobs] = useState<Job[]>(fallbackJobs)
  useEffect(() => { fetch(`${API}/work`).then(r => r.ok ? r.json() : []).then(data => Array.isArray(data) && data.length ? setJobs(data) : null).catch(() => undefined) }, [])
  return <div><section className="hero"><div className="hero-bg"/><div className="hero-overlay"/><div className="hero-content"><motion.div initial={{ opacity: 0, y: 25 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .7 }}><span className="eyebrow">DEPENDABLE. PRECISE. INDEPENDENT.</span><h1>Serious about<br /><em>your car.</em></h1><p>Premium servicing, MOT, diagnostics and repairs without the main-dealer attitude.</p><div className="hero-actions"><Link to="/contact" className="button primary">Book a Service <ArrowRight size={18} /></Link><Link to="/work" className="button ghost">See Recent Work</Link></div></motion.div><div className="hero-stats"><div><b>10+</b><span>Years experience</span></div><div><b>1:1</b><span>Expert attention</span></div><div><b>5★</b><span>Local reputation</span></div></div></div><a className="scroll-cue" href="#services"><span>Scroll to explore</span><ChevronDown size={18}/></a></section><section className="section section-intro" id="services"><Reveal><div className="section-heading"><span className="eyebrow">WHAT WE DO</span><h2>Workshop-quality work,<br /><span>without the theatre.</span></h2><p>Every job starts with proper diagnosis, clear communication and a plan that makes sense for the car — and for you.</p></div></Reveal><div className="service-grid">{services.map(({ icon: Icon, title, text }, i) => <Reveal key={title} delay={i * .08}><article className="service-card"><div className="icon-box"><Icon size={22}/></div><div><span className="service-number">0{i + 1}</span><h3>{title}</h3><p>{text}</p></div><ArrowRight className="service-arrow" size={20}/></article></Reveal>)}</div></section><section className="feature-band"><div className="feature-photo"/><div className="feature-copy"><span className="eyebrow">THE MHR WAY</span><h2>No drama.<br /><span>Just good mechanics.</span></h2><p>From a warning light that won't quit to a car that's due its annual service, we keep things simple: inspect properly, explain clearly, fix correctly.</p><div className="feature-list"><span><CheckCircle2 size={17}/> Honest recommendations</span><span><CheckCircle2 size={17}/> Modern diagnostics</span><span><CheckCircle2 size={17}/> Quality parts & workmanship</span></div><Link to="/mechanic" className="text-link">Meet the mechanic <ArrowRight size={17}/></Link></div></section><section className="section work-preview"><Reveal><div className="split-heading"><div><span className="eyebrow">RECENT WORK</span><h2>Cars in.<br /><span>Better cars out.</span></h2></div><Link to="/work" className="button ghost">View full gallery</Link></div></Reveal><div className="work-grid">{jobs.slice(0,3).map((job, i) => <Reveal key={job.id ?? job.title} delay={i * .08}><Link to="/work" className="job-card"><div className="job-image" style={{ backgroundImage: `url(${job.image_url})` }}><span>{job.category}</span></div><div className="job-copy"><div><h3>{job.title}</h3><p>{job.vehicle}</p></div><ArrowUpRight size={20}/></div></Link></Reveal>)}</div></section><section className="reviews-band" id="reviews"><div className="reviews-copy"><span className="eyebrow">CUSTOMER REVIEWS</span><h2>Good work.<br /><span>Good feedback.</span></h2><p>See the latest customer feedback for MHR Auto Services on Google.</p><div className="review-actions"><a className="button primary" href={GOOGLE_REVIEWS_URL} target="_blank" rel="noreferrer">Read our Google reviews <ArrowUpRight size={17}/></a><a className="button ghost" href={GOOGLE_REVIEWS_URL} target="_blank" rel="noreferrer">Leave a review <ArrowRight size={17}/></a></div></div><div className="reviews-card"><div className="reviews-mark">Google</div><strong>Reviews</strong><p>Open our Google profile to see the current rating and customer reviews.</p><a href={GOOGLE_REVIEWS_URL} target="_blank" rel="noreferrer">View reviews <ArrowUpRight size={16}/></a></div></section><section className="cta-band"><div><span className="eyebrow">READY WHEN YOU ARE</span><h2>Let's get your car<br /><span>back on form.</span></h2></div><Link to="/contact" className="button primary">Contact MHR <ArrowRight size={18}/></Link></section></div>
}

function Work() {
  const [jobs, setJobs] = useState<Job[]>(fallbackJobs); const [selected, setSelected] = useState<Job | null>(null)
  useEffect(() => { fetch(`${API}/work`).then(r => r.ok ? r.json() : []).then(data => Array.isArray(data) && data.length ? setJobs(data) : null).catch(() => undefined) }, [])
  return <section className="section page-section"><div className="page-intro"><span className="eyebrow">RECENT WORK</span><h1>The workshop,<br /><em>in motion.</em></h1><p>A snapshot of the jobs we’re proud to put back on the road.</p></div><div className="gallery-grid">{jobs.map((job, i) => <Reveal key={job.id ?? job.title} delay={i * .06}><button className="gallery-card" onClick={() => setSelected(job)}><div className="gallery-image" style={{ backgroundImage: `url(${job.image_url})` }}><span>{job.category}</span></div><div className="gallery-copy"><div><h3>{job.title}</h3><p>{job.vehicle}</p></div><ArrowUpRight size={20}/></div></button></Reveal>)}</div>{selected && <div className="lightbox" role="dialog" aria-modal="true" onClick={() => setSelected(null)}><button className="lightbox-close" onClick={() => setSelected(null)}><X /></button><div className="lightbox-card" onClick={e => e.stopPropagation()}><img src={selected.image_url} alt={selected.title} /><div><span className="eyebrow">{selected.category}</span><h2>{selected.title}</h2><p>{selected.vehicle} · {selected.description}</p></div></div></div>}</section>
}
function Mechanic() { return <section className="section page-section"><div className="mechanic-grid"><div className="mechanic-photo"/><div className="mechanic-copy"><span className="eyebrow">MEET THE MECHANIC</span><h1>Good cars deserve<br /><em>good people.</em></h1><p className="lead">MHR is a small independent garage built around the idea that you should know who is working on your car, what they're doing, and why.</p><p>After years working across everyday cars, performance models and commercial vehicles, the focus is always the same: diagnose properly, repair cleanly, and give customers advice they can actually trust.</p><div className="credential-grid"><div><b>10+</b><span>Years in the trade</span></div><div><b>OE</b><span>Quality components</span></div><div><b>LIVE</b><span>Diagnostic capability</span></div><div><b>1:1</b><span>Personal service</span></div></div><blockquote>“I’d rather explain why you need something than try to sell you something.”</blockquote><Link to="/contact" className="button primary">Talk about your car <ArrowRight size={18}/></Link></div></div></section> }

function Contact() {
  const [sent, setSent] = useState(false); const [busy, setBusy] = useState(false); const [error, setError] = useState('')
  async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setBusy(true); setError(''); const form = new FormData(e.currentTarget); const body = Object.fromEntries(form.entries()); try { const r = await fetch(`${API}/contact`, { method: 'POST', headers: {'content-type':'application/json'}, body: JSON.stringify(body) }); if (!r.ok) throw new Error((await r.json()).error || 'Could not send enquiry'); setSent(true) } catch (err) { setError(err instanceof Error ? err.message : 'Could not send enquiry') } finally { setBusy(false) } }
  return <section className="section page-section contact-section"><div className="page-intro"><span className="eyebrow">CONTACT MHR</span><h1>Let's talk<br /><em>cars.</em></h1><p>Tell us what you need and we'll come back to you with the next sensible step.</p></div><div className="contact-grid"><div className="contact-details"><div className="detail"><MapPin size={20}/><div><span>Garage</span><strong>Harbour Industrial Estate<br />Your Town, UK</strong></div></div><div className="detail"><Phone size={20}/><div><span>Phone</span><strong>01234 567 890</strong></div></div><div className="detail"><Mail size={20}/><div><span>Email</span><strong>hello@mhrautoservices.co.uk</strong></div></div><div className="detail"><Clock3 size={20}/><div><span>Opening hours</span><strong>Mon–Fri · 08:00–18:00<br />Sat · 09:00–13:00</strong></div></div><div className="socials"><a aria-label="Instagram" href="#"><Instagram size={19}/></a><a aria-label="Email" href="mailto:hello@mhrautoservices.co.uk"><Mail size={19}/></a></div></div><form className="contact-form" onSubmit={submit}>{sent ? <div className="success-state"><Sparkles size={30}/><h2>Message received.</h2><p>Thanks — your enquiry has been sent to the garage.</p><button className="button ghost" type="button" onClick={() => setSent(false)}>Send another</button></div> : <><div className="form-row"><label>Name<input name="name" required placeholder="Your name" /></label><label>Email<input name="email" type="email" required placeholder="you@example.com" /></label></div><label>What do you need?<textarea name="message" required rows={6} placeholder="Tell us about the car, fault, MOT, service or repair…" /></label>{error && <p style={{color:'#ff7a45',fontSize:12,marginTop:-8}}>{error}</p>}<button className="button primary" type="submit" disabled={busy}>{busy ? 'Sending…' : 'Send enquiry'} <ArrowRight size={18}/></button></>}</form></div><div className="map-placeholder"><div><MapPin size={22}/><strong>Workshop location</strong><span>Replace this placeholder with the exact garage address and map embed before launch.</span></div></div></section>
}

function Login({ onLoggedIn }: { onLoggedIn: () => void }) {
  const [email,setEmail] = useState(''); const [password,setPassword] = useState(''); const [error,setError] = useState(''); const [busy,setBusy] = useState(false); const navigate = useNavigate()
  async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch(`${API}/login`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Login failed');sessionStorage.setItem('mhr_admin_token',data.token);onLoggedIn();navigate('/admin')}catch(err){setError(err instanceof Error?err.message:'Login failed')}finally{setBusy(false)}}
  return <section className="section page-section admin-page"><div className="admin-card"><span className="eyebrow">PRIVATE AREA</span><h1>Garage admin.</h1><p>Sign in to manage work, generate invoices and keep your records in one place.</p><form className="admin-login-form" onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required /></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required /></label>{error&&<p className="form-error">{error}</p>}<button className="button primary" disabled={busy}>{busy?'Signing in…':'Sign in'} <ArrowRight size={18}/></button></form></div></section>
}

function Admin() {
  const navigate = useNavigate(); const [logged,setLogged] = useState(Boolean(sessionStorage.getItem('mhr_admin_token')))
  if(!logged) return <Login onLoggedIn={()=>setLogged(true)}/>
  return <AdminDashboard onLogout={()=>{sessionStorage.removeItem('mhr_admin_token');setLogged(false);navigate('/admin')}}/>
}

function AdminDashboard({onLogout}:{onLogout:()=>void}) {
  const [jobs,setJobs]=useState<Job[]>([]); const [title,setTitle]=useState(''); const [vehicle,setVehicle]=useState(''); const [category,setCategory]=useState('General'); const [description,setDescription]=useState(''); const [image,setImage]=useState<File|null>(null); const [invoice,setInvoice]=useState({customer_name:'',customer_email:'',vehicle:'',phone:'',job_description:'',labour:'',parts:'',vat:'20'}); const [invoiceResult,setInvoiceResult]=useState(''); const [invoiceError,setInvoiceError]=useState(''); const [busy,setBusy]=useState(false)
  const auth=()=>({Authorization:`Bearer ${sessionStorage.getItem('mhr_admin_token')||''}`})
  useEffect(()=>{fetch(`${API}/work`,{headers:auth()}).then(r=>r.ok?r.json():[]).then(data=>setJobs(Array.isArray(data)?data:[])).catch(()=>undefined)},[])
  async function addJob(e:React.FormEvent){e.preventDefault();setBusy(true);try{let image_key='';if(image){const fd=new FormData();fd.append('file',image);const r=await fetch(`${API}/upload`,{method:'POST',headers:auth(),body:fd});const d=await r.json();if(!r.ok)throw new Error(d.error||'Upload failed');image_key=d.key}const r=await fetch(`${API}/work`,{method:'POST',headers:{...auth(),'content-type':'application/json'},body:JSON.stringify({title,vehicle,category,description,image_key})});if(!r.ok)throw new Error((await r.json()).error||'Could not add job');setTitle('');setVehicle('');setDescription('');setImage(null);const data=await r.json();setJobs(j=>[data,...j])}catch(err){alert(err instanceof Error?err.message:'Could not add job')}finally{setBusy(false)}}
  async function generateInvoice(e:React.FormEvent){e.preventDefault();setBusy(true);setInvoiceError('');setInvoiceResult('');try{const r=await fetch(`${API}/invoices`,{method:'POST',headers:{...auth(),'content-type':'application/json'},body:JSON.stringify(invoice)});const data=await r.json();if(!r.ok)throw new Error(data.error||'Could not generate invoice');const pdf=await fetch(`${API}/invoice-pdf?key=${encodeURIComponent(data.pdf_key)}`,{headers:auth()});if(!pdf.ok)throw new Error('PDF could not be opened');const blob=await pdf.blob();window.open(URL.createObjectURL(blob),'_blank','noopener,noreferrer');setInvoiceResult(`${data.invoice_number} · ${invoice.customer_name} · £${Number(data.total).toFixed(2)} · generated`)}catch(err){setInvoiceError(err instanceof Error?err.message:'Could not generate invoice')}finally{setBusy(false)}}
  return <section className="section page-section admin-dashboard"><div className="admin-dashboard-header"><div><span className="eyebrow">PRIVATE AREA</span><h1>Garage dashboard.</h1><p>Manage public work and create clean invoice PDFs.</p></div><button className="button ghost" onClick={onLogout}>Logout</button></div><div className="admin-panel"><span className="eyebrow">RECENT WORK</span><h2>Add a job</h2><form className="admin-job-form" onSubmit={addJob}><div className="admin-form-main"><label>Title<input value={title} onChange={e=>setTitle(e.target.value)} required /></label><label>Vehicle<input value={vehicle} onChange={e=>setVehicle(e.target.value)} /></label><label>Category<select value={category} onChange={e=>setCategory(e.target.value)}><option>General</option><option>Servicing</option><option>Repairs</option><option>Diagnostics</option><option>MOT</option><option>Brakes</option><option>Chassis</option></select></label><label>Description<textarea value={description} onChange={e=>setDescription(e.target.value)} rows={4}/></label></div><div className="admin-form-side"><label>Photo<input type="file" accept="image/*" onChange={e=>setImage(e.target.files?.[0]||null)}/></label><button className="button primary" disabled={busy}>{busy?'Saving…':'Add to gallery'} <ArrowRight size={17}/></button></div></form></div><div className="admin-panel"><span className="eyebrow">INVOICING</span><h2>Generate invoice</h2><form className="admin-invoice-form" onSubmit={generateInvoice}><div className="invoice-grid"><label>Customer<input value={invoice.customer_name} onChange={e=>setInvoice({...invoice,customer_name:e.target.value})} required/></label><label>Customer email<input type="email" value={invoice.customer_email} onChange={e=>setInvoice({...invoice,customer_email:e.target.value})}/></label><label>Vehicle<input value={invoice.vehicle} onChange={e=>setInvoice({...invoice,vehicle:e.target.value})}/></label><label>Phone<input value={invoice.phone} onChange={e=>setInvoice({...invoice,phone:e.target.value})}/></label></div><label>Job description<textarea value={invoice.job_description} onChange={e=>setInvoice({...invoice,job_description:e.target.value})} rows={5}/></label><div className="invoice-costs"><label>Labour (£)<input type="number" min="0" step="0.01" value={invoice.labour} onChange={e=>setInvoice({...invoice,labour:e.target.value})}/></label><label>Parts (£)<input type="number" min="0" step="0.01" value={invoice.parts} onChange={e=>setInvoice({...invoice,parts:e.target.value})}/></label><label>VAT (%)<input type="number" min="0" step="0.01" value={invoice.vat} onChange={e=>setInvoice({...invoice,vat:e.target.value})}/></label></div>{invoiceError&&<p className="form-error">{invoiceError}</p>}{invoiceResult&&<p className="form-success">{invoiceResult}</p>}<button className="button primary" disabled={busy}>{busy?'Generating…':'Create & preview PDF'} <ArrowRight size={17}/></button></form></div><div className="admin-panel"><span className="eyebrow">PUBLISHED WORK</span><h2>Recent jobs</h2><div className="admin-job-list">{jobs.map(job=><div key={job.id} className="admin-job-row"><div><strong>{job.title}</strong><span>{job.vehicle} · {job.category}</span></div><span>{job.description}</span></div>)}</div></div></section>
}

export default function App(){return <Routes><Route path="*" element={<Shell><Routes><Route path="/" element={<Home/>}/><Route path="/work" element={<Work/>}/><Route path="/mechanic" element={<Mechanic/>}/><Route path="/contact" element={<Contact/>}/><Route path="/admin" element={<Admin/>}/></Routes></Shell>}/></Routes>}
